{
  "name": "post-its-desafio",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.8.3",
    "parcel": "^2.7.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html --port 8000",
    "build": "parcel build ./src/index.html --dist-dir build",
    "deploy": "gh-pages -d build"
  }
}
